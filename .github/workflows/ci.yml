name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      purge_cache:
        description: Purge the entire edge cache without publishing
        default: false
        type: boolean
      publish_compute:
        description: Publish Compute after confirming the required outer VCL is active
        default: false
        type: boolean

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  FASTLY_SERVICE_ID: pOvEEWykEbpnylqst1KTrR

jobs:
  test:
    runs-on: ubuntu-24.04-16core
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        run: |
          rustup component add clippy
          rustup target add wasm32-wasip1

      - uses: Swatinem/rust-cache@v2
        with:
          key: wasm32-wasip1

      - name: Check edge crate
        run: cargo check --tests --locked

      # Viceroy supplies the Fastly host functions the edge crate links against,
      # which plain `cargo test` on the host target cannot provide.
      # Release assets on this tag are mutable, so pin the digest as well as the
      # version: a substituted binary could otherwise fake the test output the
      # step below relies on. Update both together when bumping.
      - name: Install Viceroy
        env:
          VICEROY_VERSION: v0.20.1
          VICEROY_SHA256: c358540ae3054b0f3203125da47a76d50d2facd41cc1ac009a7e7d1f332b0437
        run: |
          curl --retry 3 --retry-all-errors -sSfL -o /tmp/viceroy.tar.gz \
            "https://github.com/fastly/Viceroy/releases/download/${VICEROY_VERSION}/viceroy_${VICEROY_VERSION}_linux-amd64.tar.gz"
          echo "${VICEROY_SHA256}  /tmp/viceroy.tar.gz" | sha256sum -c -
          tar xzf /tmp/viceroy.tar.gz -C /usr/local/bin/
          viceroy --version

      - name: Run edge crate tests under Viceroy
        run: ./scripts/run-edge-tests.sh

      - name: Run blossom-core tests
        run: cargo test -p blossom-core --locked

      - name: Run upload service tests
        run: cargo test --manifest-path cloud-run-upload/Cargo.toml --locked

      - name: Run transcoder service tests
        run: cargo test --manifest-path cloud-run-transcoder/Cargo.toml --locked

      - name: Run transcoder Python tests
        working-directory: cloud-run-transcoder
        run: python3 -m unittest discover -s tests -p 'test_*.py'

      - name: Run script Python tests
        run: python3 -m unittest discover -s scripts/tests -p 'test_*.py'

      - name: Run clippy
        run: cargo clippy --locked --all-targets --all-features

  docker:
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-24.04-16core
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha
            type=raw,value=latest

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.local
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy-upload:
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-24.04-16core
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Detect upload service changes
        id: upload-changes
        env:
          BEFORE: ${{ github.event.before }}
        run: |
          if [ "$BEFORE" = "0000000000000000000000000000000000000000" ] || \
             ! git cat-file -e "$BEFORE^{commit}" || \
             ! git diff --quiet "$BEFORE" "$GITHUB_SHA" -- cloud-run-upload; then
            echo "deploy=true" >> "$GITHUB_OUTPUT"
          else
            echo "deploy=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Authenticate to Google Cloud
        if: steps.upload-changes.outputs.deploy == 'true'
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}

      - name: Set up Cloud SDK
        if: steps.upload-changes.outputs.deploy == 'true'
        uses: google-github-actions/setup-gcloud@v2

      - name: Deploy upload service before dependent edge code
        if: steps.upload-changes.outputs.deploy == 'true'
        working-directory: cloud-run-upload
        env:
          PROJECT_ID: rich-compiler-479518-d2
        run: ./deploy.sh

      - name: Verify batch cleanup contract is active
        run: |
          curl --retry 10 --retry-all-errors --retry-delay 3 -fsS \
            https://blossom-upload-rust-149672065768.us-central1.run.app/delete-blobs/ready \
            | jq -e '.status == "ready" and .contract == "vanish-batch-v1" and .max_hashes == 20 and .vanish_audit == "authenticated-v1"'

  deploy:
    needs: [test, deploy-upload]
    if: >-
      always() &&
      needs.test.result == 'success' &&
      github.ref == 'refs/heads/main' &&
      ((github.event_name == 'push' && needs.deploy-upload.result == 'success') ||
       (github.event_name == 'workflow_dispatch' && inputs.publish_compute && !inputs.purge_cache))
    concurrency:
      group: fastly-deploy
      cancel-in-progress: false
    runs-on: ubuntu-24.04-16core
    steps:
      - uses: actions/checkout@v4

      - name: Verify required outer VCL is active
        env:
          FASTLY_OUTER_DIAGNOSTICS_ACTIVE: ${{ vars.FASTLY_OUTER_DIAGNOSTICS_ACTIVE }}
        run: |
          if [ "$FASTLY_OUTER_DIAGNOSTICS_ACTIVE" != "true" ]; then
            echo "::error::Set FASTLY_OUTER_DIAGNOSTICS_ACTIVE=true only after activating the required outer VCL."
            exit 1
          fi

      # TODO: replace with fastly/compute-actions when it supports wasm32-wasip1
      - name: Install Fastly CLI
        run: |
          curl -sL https://github.com/fastly/cli/releases/download/v14.0.4/fastly_v14.0.4_linux-amd64.tar.gz \
            | tar xz -C /usr/local/bin/

      - name: Install Rust toolchain
        run: rustup target add wasm32-wasip1

      - uses: Swatinem/rust-cache@v2
        with:
          key: wasm32-wasip1

      - name: Verify batch cleanup contract is active
        run: |
          curl --retry 10 --retry-all-errors --retry-delay 3 -fsS \
            https://blossom-upload-rust-149672065768.us-central1.run.app/delete-blobs/ready \
            | jq -e '.status == "ready" and .contract == "vanish-batch-v1" and .max_hashes == 20 and .vanish_audit == "authenticated-v1"'

      - name: Deploy to Fastly
        env:
          FASTLY_API_TOKEN: ${{ secrets.FASTLY_API_TOKEN }}
        run: fastly compute publish --token "$FASTLY_API_TOKEN" --service-id "$FASTLY_SERVICE_ID" --comment "deploy from ${{ github.sha }}"

  # Purging everything invalidates a 365-day edge TTL across the whole catalogue,
  # so keep this separate from publishing and require an explicit manual input.
  # Prefer targeted Surrogate-Key purges for individual content-addressed blobs.
  purge-cache:
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'workflow_dispatch' && inputs.purge_cache && !inputs.publish_compute
    concurrency:
      group: fastly-deploy
      cancel-in-progress: false
    runs-on: ubuntu-24.04-16core
    steps:
      - name: Install Fastly CLI
        run: |
          curl -sL https://github.com/fastly/cli/releases/download/v14.0.4/fastly_v14.0.4_linux-amd64.tar.gz \
            | tar xz -C /usr/local/bin/

      - name: Purge CDN cache
        env:
          FASTLY_API_TOKEN: ${{ secrets.FASTLY_API_TOKEN }}
        run: fastly purge --all --service-id "$FASTLY_SERVICE_ID" --token "$FASTLY_API_TOKEN"

  deploy-process-blob:
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-24.04-16core
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Deploy process-blob to Cloud Run
        working-directory: cloud-functions/process-blob
        env:
          GCP_RUNTIME_SERVICE_ACCOUNT: ${{ secrets.GCP_RUNTIME_SERVICE_ACCOUNT }}
        run: |
          if [ -z "$GCP_RUNTIME_SERVICE_ACCOUNT" ]; then
            echo "GCP_RUNTIME_SERVICE_ACCOUNT is required"
            exit 1
          fi

          gcloud run deploy process-blob \
            --project="${{ secrets.GCP_PROJECT_ID }}" \
            --region=us-central1 \
            --source=. \
            --service-account="$GCP_RUNTIME_SERVICE_ACCOUNT" \
            --memory=1Gi \
            --timeout=300s \
            --no-allow-unauthenticated \
            --update-env-vars="C2PA_MODE=off,METADATA_WEBHOOK_URL=${{ secrets.METADATA_WEBHOOK_URL }},METADATA_WEBHOOK_SECRET=${{ secrets.METADATA_WEBHOOK_SECRET }}"
